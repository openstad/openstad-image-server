version: '3.0'
services:
  db:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
  app:
    build: .
    container_name: openstad_image_server_1
    ports:
      - "3333:3333"
    links:
      - "db"
